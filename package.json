{
  "name": "ai-key-scheduler-monorepo",
  "version": "0.3.2",
  "private": true,
  "description": "AI Key Scheduler packages for rate-limit aware API key scheduling across providers and models.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Amon20044/AI-Key-Scheduler.git"
  },
  "bugs": {
    "url": "https://github.com/Amon20044/AI-Key-Scheduler/issues"
  },
  "homepage": "https://github.com/Amon20044/AI-Key-Scheduler#readme",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "npm run clean --workspaces --if-present",
    "build": "npm run build --workspace @ai-key-scheduler/core && npm run build --workspace @ai-key-scheduler/ai-sdk && npm run build --workspace @ai-key-scheduler/langchain",
    "lint": "npm run lint --workspace @ai-key-scheduler/core && npm run lint --workspace @ai-key-scheduler/ai-sdk && npm run lint --workspace @ai-key-scheduler/langchain",
    "prepublishOnly": "npm run lint && npm run typecheck && npm test && npm run test:security && npm run build",
    "test": "npm run test --workspace @ai-key-scheduler/core && npm run test --workspace @ai-key-scheduler/ai-sdk && npm run test --workspace @ai-key-scheduler/langchain",
    "test:security": "npm run test:security --workspace @ai-key-scheduler/core",
    "typecheck": "npm run build --workspace @ai-key-scheduler/core && npm run typecheck --workspace @ai-key-scheduler/core && npm run typecheck --workspace @ai-key-scheduler/ai-sdk && npm run typecheck --workspace @ai-key-scheduler/langchain"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.5",
    "vitest": "^4.1.5"
  }
}
